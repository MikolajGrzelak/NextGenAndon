version: '3.9'

services:
  andon-signals:
    build: .
    container_name: andon-signals
    restart: always
    volumes:
      - .:/app
      - sqlite_data:/app/db.sqlite3  # Dodanie wolumenu na bazę SQLite
    ports:
      - "8000:8000"
    env_file:
      - .env


volumes:
  sqlite_data:  # Definicja wolumenu dla SQLite
